name: Publish GitHub Pages

on:
  push:
    branches: [master]
    paths:
      - ".github/workflows/pages.yml"
      - "reports/final/**"
      - "site/**"
      - "site_content/**"
      - "src/healthcare_report/**"
      - "config/**"
      - "inputs/companies.md"
      - "pyproject.toml"
      - "requirements.lock"
  workflow_dispatch:
    inputs:
      rebuild_site:
        description: Rebuild the site from tracked reports before deployment
        required: false
        default: true
        type: boolean
  workflow_call:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: github-pages
  cancel-in-progress: false

jobs:
  build:
    name: Build Pages site
    if: github.ref_name == github.event.repository.default_branch
    runs-on: ubuntu-latest
    steps:
      - name: Check out the latest default branch
        uses: actions/checkout@v4
        with:
          ref: ${{ github.ref_name }}

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: requirements.lock

      - name: Install site dependencies
        run: |
          python -m pip install -r requirements.lock
          python -m pip install --no-deps -e .

      - name: Verify the locally built site
        if: github.event_name == 'push' || github.event_name == 'workflow_call'
        run: test -f site/index.html

      - name: Rebuild the static site as a fallback
        if: github.event_name == 'workflow_dispatch' && inputs.rebuild_site
        run: python -m healthcare_report build-site

      - name: Configure GitHub Pages
        uses: actions/configure-pages@v5

      - name: Upload the Pages artifact
        uses: actions/upload-pages-artifact@v4
        with:
          path: site

  deploy:
    name: Deploy GitHub Pages
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy
        id: deployment
        uses: actions/deploy-pages@v4
